# 实验方案模板

> 填写此文件后发给 Claude，调用 `/expcodegen` 即可自动生成实验 Notebook。

---

## 1. 实验信息

- **实验类型**：`<实验类型标识，英文/拼音，如 magneto_optical_kerr>`
- **实验目的**：`<一句话描述>`

---

## 2. 涉及物理量

> 下面列出实验中涉及的所有物理量，注明是**扫描变量**还是**固定值**。
> 物理量名称需与 `params/mapping.yaml` 中的 key **完全一致**。

### 扫描变量

| 物理量 (mapping key) | 起始值 | 终止值 | 点数 | 单位 |
|----------------------|--------|--------|------|------|
| `main_magnetic_field` | 0 | 10 | 20 | mA |
|                      |        |        |      |      |

### 固定参数

| 物理量 (mapping key) | 设定值 | 单位 | 备注 |
|----------------------|--------|------|------|
| `Pump_laser_power` | 1.0 | V |     |
| `temperature` | 80 | °C |     |

---

## 3. 数据采集

| 记录内容 | 对应 mapping key | 说明 |
|---------|-----------------|------|
| 锁相幅度 R | `lockin_r` | 每个扫描点读一次 |
| 示波器波形 | `scope_waveform` | 每个扫描点采一帧 |
|          |                 |      |

---

## 4. 实验时序

| 参数 | 值 | 单位 | 说明 |
|------|----|------|------|
| 扫描前等待 | 5 | s | 让温度/磁场稳定后再开始扫描 |
| 每点设置后等待 | 0.5 | s | 设置扫描值后等待响应稳定 |
| 点间间隔 | 0.1 | s | 相邻扫描点之间的额外延迟 |
| 每点平均次数 | 3 | 次 | 每个点重复采集取平均 |

---

## 5. 特殊说明（可选）

> 补充上述表格无法覆盖的信息，例如：
> - 扫描中途需要切换某个仪器的工作模式
> - 某些步骤需要手动操作
> - 需要先执行 A 再执行 B 的特定顺序
> - 异常情况处理

```
<在此处填写>
```
